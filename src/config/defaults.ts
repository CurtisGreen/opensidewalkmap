import { ViewportProps } from "@/types/ViewportProps";

/**
 * defaultViewport includes default settings required for MainMap to function:
 * - latitude
 * - longitude
 * - zoom level
 */
export const defaultViewport: ViewportProps = {
  longitude: -96.8251181,
  latitude: 32.9610115,
  zoom: 16,
};
